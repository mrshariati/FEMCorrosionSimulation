#Finite Element
cell = triangle;
H = FiniteElement("CG", cell, 1)

ci = TrialFunction(H)
v = TestFunction(H)

Ii = Coefficient(H)
Di = Constant(cell)

#Create stiffness matrix
a = Di*ci.dx(1)*v.dx(1)*dx
L = Ii*v*ds

# ffc -l dolfin DiffusionStiffnessM.ufl
