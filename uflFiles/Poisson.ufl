cell = triangle;
H = FiniteElement("CG", cell, 1)
phi = TrialFunction(H)
v = TestFunction(H)

kappa = Coefficient(H)
f = Coefficient(H)
g = Coefficient(H)

a = kappa*inner(grad(phi), grad(v))*dx
L = f*v*dx + kappa*g*v*ds

# ffc -l dolfin Poisson.ufl
